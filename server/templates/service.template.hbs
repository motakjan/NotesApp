import  {{titleCase name}}Model from '../models/{{snakeCase name}}.model';

export const findAll = async () => {
  return await  {{titleCase name}}Model.find();
};

export const findOne = async (id: string) => {
  return await  {{titleCase name}}Model.findById(id);
};

export const createOne = async (data: any) => {
  const newTest = new  {{titleCase name}}Model({
    ...data,
  });

  return await newTest.save();
};

export const updateOne = async (id: string, data: any) => {
  return await  {{titleCase name}}Model.findByIdAndUpdate(id, data, {
    new: true,
  });
};

export const deleteOne = async (id: string) => {
  return await  {{titleCase name}}Model.findByIdAndDelete(id);
};




