import * as mongoose from 'mongoose';

const {{titleCase name}}Schema = new mongoose.Schema(
    {

    },
    { timestamps: true }
);

const {{titleCase name}}Model = mongoose.model('{{titleCase name}}', {{titleCase name}}Schema);

export default {{titleCase name}}Model;
